import React from 'react';

const clock = (props) => {
    return (
        <div>
            <h1>Hello, world!</h1>
            <h2>It is {props.date}.</h2>
        </div>
    )
};

export default clock;
